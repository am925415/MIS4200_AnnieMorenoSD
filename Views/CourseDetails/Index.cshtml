@model IEnumerable<MIS4200_AnnieMorenoSD.Models.CourseDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Professors.professorFirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.courseDetails)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.coursePrice)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Professors.professorFirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.courseDetails)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.coursePrice)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.courseDetailID }) |
            @Html.ActionLink("Details", "Details", new { id=item.courseDetailID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.courseDetailID })
        </td>
    </tr>
}

</table>
